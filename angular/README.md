# Módulo Angular — Panel de doctor

Módulo individual de Alex (Angular). Ver el README principal del repo para
arquitectura, backend compartido y cómo correr el proyecto integrado.

```bash
npm install
npm start   # http://localhost:4200
```
