{
  "name": "saludasist-monorepo",
  "private": true,
  "description": "Orquestador del monorepo: arma el deploy integrado (contenedora + vue + angular) para Vercel/Netlify. Cada módulo tiene su propio package.json.",
  "scripts": {
    "build:integrado": "node scripts/build-integrado.mjs"
  }
}
